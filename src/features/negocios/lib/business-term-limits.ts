/** Official max business term (plazo); UI + server MUST stay aligned. */
export const BUSINESS_TERM_MAX = 25 as const

/** Seed/catalog name for yearly purchase periodicity (see `prisma/seeds/buy-periodicity.ts`). */
export const BUY_PERIODICITY_ANUAL_NAME = 'Anual' as const
