# Tasks: Mejora Sincronización por Período

## Phase 1: Database & Types Foundation

- [x] 1.1 In `prisma/schema.prisma`, add `month Int? @map("month")` and `year Int? @map("year")` to the `FileImport` model, and add a compound index `@@index([fileType, month, year, status])` on that same model.
- [x] 1.2 In `prisma/schema.prisma`, add `resolved Boolean @default(false) @map("resolved")` and `resolvedAt DateTime? @map("resolved_at")` to the `FileImportError` model.
- [x] 1.3 In `prisma/schema.prisma`, add `syncDate DateTime? @map("sync_date")` to the `SettlementCommission` model.
- [x] 1.4 Run `npx prisma migrate dev --name add_period_sync_fields` to generate and apply the migration for all three model changes above. <!-- BLOCKED: remote Neon DB unreachable at time of apply — schema is correct; re-run when DB is available -->
- [x] 1.5 Run `npx prisma generate` to regenerate the Prisma client with the new fields.
- [x] 1.6 In `src/features/load-file/types/load-file.types.ts`, add `month?: number` and `year?: number` to the `FileImportHistory` interface so the history list can carry period data.
- [x] 1.7 In `src/features/load-file/services/processors/processor.interface.ts`, add `resolvedErrors: number` to the `ProcessorResult` interface (set to `0` for ERROR and LAG paths; `>= 0` for SYNCHRONIZED paths).

## Phase 2: Utility & New Service Layer

- [x] 2.1 **[RED]** Create `src/features/load-file/__tests__/file-naming.test.ts` with failing Vitest tests covering: all 12 months produce correct Spanish uppercase names, `generateSyncFileName('POLIZA', 2, 2026)` returns `'SINCRONIZACION-POLIZA-FEBRERO-2026'`, `generateSyncFileName('VOLUNTARIA', 12, 2025)` returns `'SINCRONIZACION-VOLUNTARIA-DICIEMBRE-2025'`, and invalid month (0 or 13) throws an error.
- [x] 2.2 **[GREEN]** Create `src/features/load-file/lib/file-naming.ts` with the `SPANISH_MONTHS` constant map and the `generateSyncFileName(fileType: string, month: number, year: number): string` export. All tests from 2.1 must pass.
- [x] 2.3 **[RED]** Create `src/features/load-file/__tests__/file-import.service.test.ts` with failing Vitest tests covering: `initiateImport` creates new `FileImport` when none exists for the period (returns `{ created: true, fileImport }`); `initiateImport` returns existing `LOAD` import for same `fileType + month + year + idUser` without calling `prisma.fileImport.create` (returns `{ created: false, fileImport }`); `initiateImport` throws `PeriodCompletedError` when a `COMPLETED` import exists for same `fileType + month + year + idUser`; dedup does NOT trigger for a different `idUser`.
- [x] 2.4 **[GREEN]** Create `src/features/load-file/services/file-import.service.ts` implementing `FileImportService` as a class with `static async initiateImport(params: { fileType: FileType; month: number; year: number; idUser: number }): Promise<InitiateImportResult>` and `static async listFileImports(params: { userId: number; isAdmin: boolean }): Promise<FileImportHistory[]>`. The `initiateImport` method must: (1) check for a `COMPLETED` import scoped by `idUser + fileType + month + year` and throw a typed error if found; (2) check for a `LOAD` import with same scope and return it (dedup); (3) otherwise call `prisma.fileImport.create` with `nameFile` generated via `generateSyncFileName`. `listFileImports` moves the existing `prisma.fileImport.findMany` query from `route.ts` (GET handler). All tests from 2.3 must pass.

## Phase 3: Batch Processing — syncDate & Error Resolution

- [x] 3.1 In `src/features/load-file/services/processors/voluntaria.processor.ts`, add `syncDate: new Date()` to the `data` object inside the `createSync()` private method (the `settlementCommission.create` call). Verify that the `createLag()` method and the no-business/no-sync paths do NOT include `syncDate`.
- [x] 3.2 In `src/features/load-file/services/processors/voluntaria.processor.ts`, add an error-resolution step inside the `process()` method after a successful SYNCHRONIZED result: call `tx.fileImportError.updateMany({ where: { idFileImport: fileImportId, contract: record.contract, resolved: false }, data: { resolved: true, resolvedAt: new Date() } })`. Count the number of records affected and include it in the returned `ProcessorResult` as `resolvedErrors`.
- [x] 3.3 In `src/features/load-file/services/processors/poliza.processor.ts`, apply the same two changes as 3.1 and 3.2: add `syncDate: new Date()` to `createSync()`, and add error-resolution logic with `resolvedErrors` count in `ProcessorResult`.
- [x] 3.4 In `src/features/load-file/services/process-batch.service.ts`, add a `resolvedErrorsBatch` accumulator (initialized to `0`) in the per-batch loop. After each `processor.process()` call, add `result.resolvedErrors` to `resolvedErrorsBatch`. In the `prisma.fileImport.update` call at the end of each batch, change `errorRecord: { increment: errorBatch }` to `errorRecord: { increment: errorBatch, decrement: resolvedErrorsBatch }` (Prisma supports both operators in the same `update` call via separate field operations — use a raw expression or split into two fields if Prisma does not support both simultaneously on the same field; otherwise use `increment: errorBatch` in one update and `decrement: resolvedErrorsBatch` in another within the same transaction block). Also add `resolvedErrorsBatch` to the outer `resolvedErrorsTotal` running total.

## Phase 4: API Route Refactor

- [x] 4.1 In `src/app/api/carga-archivos/file-import/route.ts`, add a Zod schema `createFileImportSchema = z.object({ fileType: z.enum(['POLIZA', 'VOLUNTARIA']), month: z.number().int().min(1).max(12), year: z.number().int().min(2020).max(2100) })` and apply it to validate the POST request body. Return HTTP 400 with Zod validation errors if the body does not match.
- [x] 4.2 In `src/app/api/carga-archivos/file-import/route.ts` POST handler, remove the direct `prisma.fileImport.create` call and the `prisma.user.findUnique` user-existence check. Replace with a single call to `FileImportService.initiateImport({ fileType, month, year, idUser })`. Handle the result: return HTTP 201 if `created: true`, return HTTP 200 if `created: false` (dedup). Catch the `PeriodCompletedError` from the service and return HTTP 409 with body `{ data: null, error: "El período {month}/{year} ya fue liquidado" }`. Remove the `prisma` import from the route file entirely.
- [x] 4.3 In `src/app/api/carga-archivos/file-import/route.ts` GET handler, remove the direct `prisma.fileImport.findMany` call and replace it with `FileImportService.listFileImports({ userId, isAdmin })`. The response shape remains unchanged. Remove any remaining `prisma` import from the route file.

## Phase 5: API Client & UI

- [x] 5.1 In `src/features/load-file/lib/load-file-api.ts`, update the `initiateImport` function signature to accept `month: number` and `year: number` as required parameters (after `fileType`). Add `month` and `year` to the `JSON.stringify` POST body. The `fileName` parameter can be removed since the name is now generated server-side; if it must remain for backward compatibility, keep it but document that the server will ignore it.
- [x] 5.2 In `src/features/load-file/components/CargarArchivoTab.tsx`, extract a pure helper function `getDefaultPeriod(): { month: number; year: number }` that returns `{ month: currentMonth - 1, year: currentYear }` with January wrap-around (`month = 12, year = currentYear - 1`). Add `selectedMonth` and `selectedYear` controlled state: `const [selectedMonth, setSelectedMonth] = useState<number>(() => getDefaultPeriod().month)` and `const [selectedYear, setSelectedYear] = useState<number>(() => getDefaultPeriod().year)`.
- [x] 5.3 In `src/features/load-file/components/CargarArchivoTab.tsx`, replace the multiple `isUploading / errorMessage / errorModalOpen` states used for the initiation request lifecycle with a single `AsyncState<{ fileImport: FileImport }>` discriminated union (from `src/features/shared/types/async-state.types.ts`). This applies only to the `initiateImport` call state; existing progress tracking state (`processingProgress`, etc.) is unaffected.
- [x] 5.4 In `src/features/load-file/components/CargarArchivoTab.tsx`, add a month selector (`<select>` with options 1–12 displaying Spanish month names) and a year selector (numeric input or `<select>` with a reasonable range such as 2020–2030) below the file type selector. Bind them to `selectedMonth` and `selectedYear` state. Pass both values to `loadFileApi.initiateImport(selectedFile.name, selectedFileType, selectedMonth, selectedYear, ...)` in `handleUpload`.
- [x] 5.5 In `src/features/load-file/components/CargarArchivoTab.tsx`, handle the HTTP 409 response from `initiateImport`: transition the `AsyncState` to `{ status: 'error', error: <message from API> }` and display the blocked-period error message (e.g. inside the existing `AlertModal` or an inline error banner) so the user sees "El período {month}/{year} ya fue liquidado".

## Phase 6: Tests & Verification

- [x] 6.1 In `src/features/load-file/__tests__/process-batch.service.test.ts`, add test cases for: (a) when processors return `resolvedErrors: 3`, the `fileImport.update` call includes a decrement of 3 on `errorRecord`; (b) mixed batch where new errors and resolved errors both occur, resulting in the correct net `errorRecord` change; (c) `syncDate` is set on `SettlementCommission` records created via the SYNCHRONIZED path (assert the mock `create` call was called with `syncDate` non-null); (d) LAG records do not have `syncDate` set (assert `syncDate` is absent or `null` in the mock call). Also fixed pre-existing failures caused by missing `fileImportError.updateMany` mock.
- [x] 6.2 In `src/features/load-file/__tests__/file-import.service.test.ts` (created in 2.3), verify that the test for the `listFileImports` method also works correctly for both admin (`isAdmin: true`) and non-admin users (scoped to `userId`). Already satisfied by tests written in Phase 2.3.
- [x] 6.3 Extracted `getDefaultPeriod` to `src/features/load-file/lib/period-utils.ts` (exported with optional `now` param). Created `src/features/load-file/__tests__/period-utils.test.ts` with full coverage for January wrap-around and February–December scenarios.
- [x] 6.4 Write an integration-style test for the re-sync flow in `src/features/load-file/__tests__/process-batch.service.test.ts`: simulate a first sync that produces 2 `FileImportError` records, then simulate a second sync pass over the same `fileImportId` where both error contracts now succeed — verify `FileImportError.updateMany` is called twice with `resolved: true` and `resolvedAt` set, and `errorRecord` is decremented by 2.
- [x] 6.5 Run `npm run type-check` and fix all TypeScript errors introduced by the new fields and updated interfaces. Result: 0 errors.
- [x] 6.6 Run `npm run lint` and fix all lint violations introduced by this change. Result: 0 errors, 2 pre-existing warnings (not introduced by Phase 6).
- [x] 6.7 Run `npm run test:unit` and confirm all unit tests pass (file-naming, file-import.service, process-batch.service, CargarArchivoTab helper). Result: 110 test files, 1301 passed, 0 failed.
