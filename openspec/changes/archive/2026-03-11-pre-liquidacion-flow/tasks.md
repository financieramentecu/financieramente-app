# Tasks: Pre-liquidación flow — clawback persistence and balance by flow

## Phase 1: Foundation (types and lib)

- [x] 1.1 Add `PreLiquidacionFlow` union type (`'VOLUNTARIA' | 'POLIZA_CARTERA' | 'POLIZA_NO_CLAW' | 'POLIZA_CLAW'`) and `DeriveFlowInput` interface (readonly `commissionType`, `originCommission`, `isClawback`) to `src/features/pre-liquidacion/types/types.ts`.
- [x] 1.2 **[RED]** Create `src/features/pre-liquidacion/lib/__tests__/pre-liquidacion-flow.test.ts`: add failing tests for `deriveFlow()` — given DeriveFlowInput, expect PreLiquidacionFlow. Cover: VOLUNTARIA; POLIZA + isClawback true → POLIZA_CLAW; POLIZA + originCommission CARTERA → POLIZA_CARTERA; POLIZA + isClawback false (no CARTERA) → POLIZA_NO_CLAW; POLIZA + CARTERA + isClawback true → POLIZA_CLAW (not CARTERA).
- [x] 1.3 Create `src/features/pre-liquidacion/lib/pre-liquidacion-flow.ts` with `deriveFlow(registro: DeriveFlowInput): PreLiquidacionFlow` (pure function; no Prisma). Order: VOLUNTARIA first, then POLIZA+isClawback → POLIZA_CLAW, then POLIZA+CARTERA → POLIZA_CARTERA, then POLIZA → POLIZA_NO_CLAW, fallback VOLUNTARIA.
- [x] 1.4 **[GREEN]** Run `src/features/pre-liquidacion/lib/__tests__/pre-liquidacion-flow.test.ts`; all deriveFlow tests pass.
- [x] 1.5 Optionally add `shouldPersistClawback(flow: PreLiquidacionFlow): boolean` in `pre-liquidacion-flow.ts` (returns true when flow !== 'VOLUNTARIA') and unit test it in the same test file.

## Phase 2: Service — query and flow wiring

- [x] 2.1 In `src/features/pre-liquidacion/services/pre-liquidacion.service.ts`, extend the `findMany` for SettlementCommission (inside `procesarPreLiquidacion`) to include `business: { include: { user: true, productPercentageCommission: true } }` so each registro has `business.user.idUser`, `commissionType`, `originCommission`, `isClawback` available (latter three are already on the model).
- [x] 2.2 In the same service, at the start of the loop over registros, call `deriveFlow(registro)` with `{ commissionType: registro.commissionType, originCommission: registro.originCommission, isClawback: registro.isClawback }` and store the result in a `flow` variable. Ensure TypeScript types (use `DeriveFlowInput` / `PreLiquidacionFlow` from feature types).

## Phase 3: Service — Clawback and ClawbackBalance (add flows)

- [x] 3.1 Inside the existing `prisma.$transaction` in `procesarPreLiquidacion`, after each `tx.comissionDistribution.create`: if `flow !== 'VOLUNTARIA'` and `valorClawback > 0` (and flow is not POLIZA_CLAW), create a `Clawback` row via `tx.clawback.create` with `idComissionDistribution` = the created distribution's id (capture from create result), `idUser` = `registro.business!.user.idUser`, `valueClawback` = valorClawback, `porcentajeApplied` = clawbackPorcentaje from registro, `state: 'RETENIDO'`. Ensure `registro.business` and `registro.business.user` are non-null (already guarded earlier).
- [x] 3.2 After the loop over configCategorias (inside the same transaction), compute `totalValorClawback` = sum of `valorClawback` for all categories of this registro. If `flow !== 'VOLUNTARIA'` and `totalValorClawback > 0`: upsert ClawbackBalance for `registro.business!.user.idUser` — use `tx.clawbackBalance.findUnique({ where: { idUser } })`; if null then `tx.clawbackBalance.create({ data: { idUser, totalAmount: totalValorClawback } })`; else `tx.clawbackBalance.update({ where: { idUser }, data: { totalAmount: currentTotal.add(totalValorClawback) } })`. Use Prisma Decimal handling; one balance update per registro.
- [x] 3.3 Add unit test in `pre-liquidacion.service.test.ts`: mock one registro with `commissionType: 'POLIZA'`, `isClawback: false`, `clawbackPercentage: 0.1`, `business: { user: { idUser: 42 } }`; mock `prisma.clawback.create` and `prisma.clawbackBalance.findUnique`/`create`/`update`; run `procesarPreLiquidacion`; assert `clawback.create` called per category with valorClawback > 0, and balance updated with positive amount. Mark 3.1–3.2 **[GREEN]** when this test passes.

## Phase 4: Service — Poliza CLAW (subtract from balance)

- [ ] 4.1 Before the transaction for each registro, when `flow === 'POLIZA_CLAW'`, resolve active clawback percentage: use existing `obtenerDescuentoActivo()` (or `prisma.commissionDiscount.findFirst({ where: { status: 'ACTIVE', type: 'CLAWBACK' } })`) to get `clawbackPercentage`; if null/undefined use fallback `0.1` (Decimal). Pass this value into the transaction (e.g. `activeClawbackPercentage`).
- [ ] 4.2 Inside the transaction for POLIZA_CLAW: for each category, compute `categoryDebit = valorComisionBruta.mul(activeClawbackPercentage)`; create `ComissionDistribution` as today; then create `Clawback` with `valueClawback = categoryDebit`, `porcentajeApplied = activeClawbackPercentage`, `state: 'RETENIDO'`, `idUser = registro.business!.user.idUser`. After the loop, compute `totalDebit` = sum of categoryDebit; upsert ClawbackBalance for that user by **subtracting** totalDebit (findUnique; if null create with totalAmount = -totalDebit, else update totalAmount = current - totalDebit). Allow negative balance (no cap).
- [ ] 4.3 Add unit test: mock registro with `commissionType: 'POLIZA'`, `isClawback: true`, `business: { user: { idUser: 42 } }`; mock CommissionDiscount CLAWBACK or fallback; assert Clawback create called per category with categoryDebit, ClawbackBalance updated with negative delta. **[GREEN]** when test passes.

## Phase 5: Service — Voluntarias and edge cases

- [ ] 5.1 **[RED]** Add unit test: mock registro with `commissionType: 'VOLUNTARIA'`, `business: { user: { idUser: 1 } }`; run `procesarPreLiquidacion`; assert `comissionDistribution.create` called; assert `clawback.create` never called; assert `clawbackBalance.findUnique`/`create`/`update` never called.
- [ ] 5.2 **[GREEN]** Ensure flow === 'VOLUNTARIA' skips all Clawback and ClawbackBalance logic (no create/update). Verify test passes.
- [ ] 5.3 Add unit test: Poliza with `clawbackPercentage: 0`, `isClawback: false` — assert only ComissionDistribution created; no Clawback, no ClawbackBalance update.
- [ ] 5.4 Add unit test: transaction rollback — when `tx.clawback.create` throws, assert the transaction is rolled back (e.g. `settlementCommission.update` not called with PRE-SETTLED, or verify via mock that create counts are rolled back). Implement by not catching inside the transaction callback so Prisma rolls back.

## Phase 6: Testing and cleanup

- [ ] 6.1 Extend the existing "success and process records" test in `pre-liquidacion.service.test.ts`: add `commissionType`, `originCommission`, `isClawback` to the mock SettlementCommission; add mocks for `prisma.clawback.create` and `prisma.clawbackBalance` (findUnique, create, update) so the test does not fail; assert clawback and balance are called when clawbackPercentage > 0.
- [ ] 6.2 Run full pre-liquidacion service test suite: `npm run test -- src/features/pre-liquidacion/services/pre-liquidacion.service.test.ts` and lib test suite: `npm run test -- src/features/pre-liquidacion/lib/__tests__/pre-liquidacion-flow.test.ts`; all tests pass.
- [ ] 6.3 Verify `src/app/api/pre-liquidacion/procesar/route.ts` still has no Prisma import and no business logic (only auth, Zod, build rangoFecha, call `procesarPreLiquidacion`, return response). No code change required; checklist only.
- [ ] 6.4 If the transaction callback in the service exceeds ~50 lines, extract a helper e.g. `processRegistroTransaction(tx, registro, configCategorias, flow, comisionBase, descuentoPorcentaje, clawbackPorcentaje, usePortfolio, activeClawbackPercentage?)` and call it from inside `prisma.$transaction`; keep parameters to 3–4 or pass a single context object to stay under project guideline.

## Phase 7: Historial visibility (PRE-SETTLED and file list)

- [x] 7.1 In `src/app/api/pre-liquidacion/resultados/[fileId]/route.ts`, change the `where` filter from `status: 'PRELIQUIDADO'` to `status: 'PRE-SETTLED'` so the historial results API returns pre-liquidated records (spec: "Pre-liquidación results and export use PRE-SETTLED state").
- [x] 7.2 In `src/app/api/pre-liquidacion/exportar/[fileId]/route.ts`, change the `where` filter from `status: 'PRELIQUIDADO'` to `status: 'PRE-SETTLED'` so the export API includes pre-liquidated records (same spec requirement).
- [x] 7.3 In `src/features/pre-liquidacion/services/pre-liquidacion.service.ts`, function `obtenerArchivosDisponiblesPreliquidacion`: (a) change the `findMany` `where` so that `settlementCommissions` has `some: { status: { in: ['SYNCHRONIZED', 'PRE-SETTLED'] } }` instead of only `SYNCHRONIZED`; (b) remove or replace the single `_count` (which currently counts SYNCHRONIZED only); (c) after `findMany`, get the list of `idFileImport` from the result; run `prisma.settlementCommission.groupBy({ by: ['idFileImport', 'status'], where: { idFileImport: { in: fileIds }, status: { in: ['SYNCHRONIZED', 'PRE-SETTLED'] } }, _count: true })`; (d) build a map `idFileImport -> { sincronizados: number, registrosPreliquidados: number }` from the groupBy result; (e) when mapping to `ArchivoDisponible[]`, set `sincronizados` and `registrosPreliquidados` from this map (for each file use map entry or 0 if missing). Spec: "File list for pre-liquidación includes pending and pre-liquidated files".
- [ ] 7.4 Optional: Add unit test for `obtenerArchivosDisponiblesPreliquidacion` — mock `prisma.fileImport.findMany` to return one file, mock `prisma.settlementCommission.groupBy` to return rows with that fileId and status SYNCHRONIZED/PRE-SETTLED and counts; assert returned archivos have correct `sincronizados` and `registrosPreliquidados`. Or verify manually: after pre-liquidating, list includes the file and Histórico tab shows it.
